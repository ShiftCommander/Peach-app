## 2024-05-18 - Toast Notifications\n**Learning:** The app called UX feedback methods that did not exist, leading to silent failures on action confirmations. Implementing a dynamic toast container with `aria-live` is an effective way to handle this without cluttering the initial DOM.\n**Action:** Always ensure feedback functions called from UI actions are implemented, visually apparent, and accessible to screen readers using ARIA live regions.
